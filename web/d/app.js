const money=n=>new Intl.NumberFormat('zh-CN',{style:'currency',currency:'CNY',maximumFractionDigits:0}).format(n);
const pct=n=>`${Number(n)>=0?'+':''}${Number(n).toFixed(2)}%`;
function draw(canvas,curve){const dpr=devicePixelRatio||1,rect=canvas.getBoundingClientRect();canvas.width=rect.width*dpr;canvas.height=rect.height*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);const w=rect.width,h=rect.height,vals=curve.map(x=>x.equity);if(vals.length<2)return;const min=Math.min(...vals),max=Math.max(...vals),pad=10;c.strokeStyle='#82a8ff';c.lineWidth=2;c.beginPath();vals.forEach((v,i)=>{const x=pad+i*(w-2*pad)/(vals.length-1);const y=h-pad-(v-min)/(max-min||1)*(h-2*pad);i?c.lineTo(x,y):c.moveTo(x,y)});c.stroke();}
fetch('data.json?ver=2',{cache:'no-store'}).then(r=>r.json()).then(d=>{const ret=d.metrics.return_pct;document.querySelector('#app').innerHTML=`<div class="shell"><div class="top"><div><div class="brand">AI 综合判断基金</div><div class="muted">${d.plain_explanation||'趋势、公司、估值、动量和风险一起看，属于综合均衡型。'}</div><div class="muted">更新 ${d.updated_at}</div></div><div class="badge">${d.mode==='DEMO'?'演示数据 · ':''}市场 ${d.market_label} · ${d.market_score}</div></div><section class="hero"><div class="label">当前虚拟总资产</div><div class="equity">${money(d.fund.equity)}</div><div class="${ret>=0?'positive':'negative'}">累计 ${pct(ret)}</div><div class="stats"><div class="stat"><span class="label">今日</span><b class="${d.metrics.today_pct>=0?'positive':'negative'}">${pct(d.metrics.today_pct)}</b></div><div class="stat"><span class="label">股票仓位</span><b>${d.fund.position_pct.toFixed(1)}%</b></div><div class="stat"><span class="label">现金</span><b>${money(d.fund.cash)}</b></div></div></section><div class="section"><h2>当前持仓</h2>${d.holdings.length?d.holdings.map(x=>`<div class="card"><div class="row"><div><div class="holding-name">${x.name}</div><div class="mini">${x.symbol} · ${x.shares}股 · 成本 ${x.avg_cost.toFixed(2)}</div></div><div style="text-align:right"><b>${x.weight.toFixed(1)}%</b><div class="${x.pnl_pct>=0?'positive':'negative'} mini">${pct(x.pnl_pct)}</div></div></div><div class="progress"><i style="width:${Math.min(100,x.weight*5)}%"></i></div></div>`).join(''):'<div class="card muted">当前为空仓。系统允许什么都不买。</div>'}</div><div class="section"><h2>今日决策</h2>${d.decisions.length?d.decisions.map(x=>`<div class="card decision"><div class="row"><strong>${x.action} · ${x.name}</strong><span class="pill">${x.weight||''}</span></div><div class="reason">${x.reason}</div></div>`).join(''):'<div class="card muted">今日无交易目标调整。</div>'}</div><div class="section"><h2>成绩单</h2><div class="card"><div class="stats"><div class="stat"><span class="label">最大回撤</span><b>${pct(d.metrics.max_drawdown_pct)}</b></div><div class="stat"><span class="label">胜率</span><b>${d.metrics.win_rate_pct.toFixed(1)}%</b></div><div class="stat"><span class="label">交易</span><b>${d.metrics.trades}</b></div></div><canvas id="curve" class="chart"></canvas></div></div><div class="section"><h2>AI交易日记</h2><div class="card reason">${d.diary}</div></div></div><div class="footer"><span>100万虚拟资金 · 不连接券商 · 不存在真实下单接口</span></div>`;draw(document.querySelector('#curve'),d.equity_curve)}).catch(e=>document.querySelector('#app').innerHTML=`<div class="splash">数据加载失败：${e}</div>`);
if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js');
